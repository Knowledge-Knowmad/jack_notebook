rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 卡片資料：任何人可讀，登入後才能寫
    match /cards/{cardId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
